'use strict';
const fs=require('fs');const assert=require('assert');
const svc=fs.readFileSync('src/core/searchContextService.js','utf8');const search=fs.readFileSync('src/core/search.js','utf8');const ac=fs.readFileSync('src/core/autocompleteContextBridge.js','utf8');const loader=fs.readFileSync('api/app.js','utf8');
assert(svc.includes('HouseholdContext'),'search service must use HouseholdContext');assert(svc.includes('SEARCH_CONTEXT_CHANGED'),'search service needs stale-context error');assert(!/firebase\.database|fbFamilyId|fbUser/.test(svc),'search service must not read Firebase or legacy identity directly');assert(svc.includes("notesIncluded:false"),'notes must remain explicitly excluded');assert(svc.includes("aiAllowlist:['tasks','recipes','calendar','shopping','members']"),'AI context must be allowlisted');assert(svc.includes('sourceCurrent'),'canonical source status must be checked against current context');
['taskData','recipesData','shopData','noteData','calData'].forEach((name)=>assert(!new RegExp('\\b'+name+'\\b').test(search),'search UI must not read '+name+' directly'));assert(search.includes('SearchContextService'),'search UI must delegate to SearchContextService');assert(search.includes('searchEsc'),'search results must be escaped before HTML rendering');
assert(ac.includes('window.SearchContextService'),'autocomplete bridge must delegate through SearchContextService');assert(/\.shoppingSuggestions\s*\(/.test(ac),'autocomplete bridge must use scoped shopping suggestions');assert(/\.ingredientSuggestions\s*\(/.test(ac),'autocomplete bridge must use scoped ingredient suggestions');assert(!/\bshopData\b|\brecipesData\b/.test(ac),'autocomplete bridge must not consume household globals directly');
const a=loader.indexOf('searchContextService.js'),b=loader.indexOf('autocomplete.js?v=2'),c=loader.indexOf('autocompleteContextBridge.js'),d=loader.indexOf('search.js?v=2');assert(a>-1&&b>a&&c>b&&d>c,'loader must install service -> legacy autocomplete -> scoped bridge -> search UI');console.log('search context adoption: ok');
